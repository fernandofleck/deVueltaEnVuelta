import React, {Component} from 'react';

class Controls extends Component{
    render(){
        return (
            <div>
                <button>Play</button>
                <button>Stop</button>
                <button>Sound</button>
            </div>
        );
    };
};

export default Controls;